package ir.controller;

import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
//import ir.kimia.energyupload.service.order.OrderCancellationService;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
/*
@RestController
@RequestMapping("/api/orders")
@RequiredArgsConstructor
@Tag(name = "Order API", description = "مدیریت سفارش‌ها")

 */
public class OrderController {
/*
    private final OrderCancellationService cancellationService;

    @PostMapping("/{orderId}/cancel")
    @ResponseStatus(HttpStatus.OK)
    @Operation(
            summary = "لغو سفارش",
            description = """
                    سفارش را لغو می‌کند.
                    
                    عملیات:
                    - آزادسازی موجودی رزرو شده
                    - لغو پرداخت
                    - تغییر وضعیت سفارش به CANCELLED
                    """
    )
    public void cancelOrder(@PathVariable Long orderId) {

        cancellationService.cancel(orderId);

    }
*/
}
